@using System.Globalization @* YENİ: fiyatı JS'e noktalı yazmak için *@
@model GetProductByIdQueryResult
@{
    ViewData["Title"] = "Detail";
    Layout = "~/Views/Shared/_WebUILayout.cshtml";
}

<style>
    .single-product .product-image .image {
        margin: 0;
        background: #fff;
    }

        .single-product .product-image .image img {
            width: 100%;
            height: 520px;
            object-fit: cover;
            object-position: center;
            display: block;
        }

    .single-product .slider-pager .thumb-box li {
        margin-bottom: 20px;
    }

        .single-product .slider-pager .thumb-box li figure {
            margin: 0;
            background: #fff;
        }

        .single-product .slider-pager .thumb-box li img {
            width: 100px;
            height: 100px;
            object-fit: cover;
            object-position: center;
            display: block;
        }

    .single-product .tags.list li {
        margin-right: 10px;
    }

    .single-product .product-details-content:before,
    .single-product .product-details-content:after {
        display: none !important;
    }

</style>


<section class="page-title centred"
         style="background-image: url('@Url.Content("~/Bagery Pack/Bagery/assets/images/background/shop detail arka plan.png")');">
    <div class="auto-container">
        <div class="content-box">
            <div class="title">
                <h1>Ürün Detayı</h1>
            </div>
        </div>
    </div>
</section>
<!--End Page Title-->
<!-- single-product -->
<section class="single-product">

    

    <div class="product-details-content"
         style="background-color: red; background-image: url('@Url.Content("~/Bagery Pack/Bagery/assets/images/clients/urunArka.png")'); background-size: cover; background-position: center; background-repeat: no-repeat;">
        <div class="auto-container">
            <div class="row clearfix">
                <div class="col-lg-6 col-md-12 col-sm-12 image-column">
                    <div class="bxslider">
                        <div class="slider-content">
                            <div class="slider-pager">
                                <ul class="thumb-box">
                                    <li>
                                        <a class="active" data-slide-index="0" href="#"><figure><img src="@Model.MainImageUrl" alt="@Model.ProductName"></figure></a>
                                    </li>
                                    <li>
                                        <a data-slide-index="1" href="#"><figure><img src="@Model.ImageUrl1" alt="@Model.ProductName"></figure></a>
                                    </li>
                                    <li>
                                        <a data-slide-index="2" href="#"><figure><img src="@Model.ImageUrl2" alt="@Model.ProductName"></figure></a>
                                    </li>
                                </ul>
                            </div>
                            <div class="product-image">
                                <figure class="image"><a href="@Model.MainImageUrl" class="lightbox-image" data-fancybox="gallery"><img src="@Model.MainImageUrl" alt="@Model.ProductName"></a></figure>
                            </div>
                        </div>
                        <div class="slider-content">
                            <div class="slider-pager">
                                <ul class="thumb-box">
                                    <li>
                                        <a data-slide-index="0" href="#"><figure><img src="@Model.MainImageUrl" alt="@Model.ProductName"></figure></a>
                                    </li>
                                    <li>
                                        <a class="active" data-slide-index="1" href="#"><figure><img src="@Model.ImageUrl1" alt="@Model.ProductName"></figure></a>
                                    </li>
                                    <li>
                                        <a data-slide-index="2" href="#"><figure><img src="@Model.ImageUrl2" alt="@Model.ProductName"></figure></a>
                                    </li>
                                </ul>
                            </div>
                            <div class="product-image">
                                <figure class="image"><a href="@Model.ImageUrl1" class="lightbox-image" data-fancybox="gallery"><img src="@Model.ImageUrl1" alt="@Model.ProductName"></a></figure>
                            </div>
                        </div>
                        <div class="slider-content">
                            <div class="slider-pager">
                                <ul class="thumb-box">
                                    <li>
                                        <a data-slide-index="0" href="#"><figure><img src="@Model.MainImageUrl" alt="@Model.ProductName"></figure></a>
                                    </li>
                                    <li>
                                        <a data-slide-index="1" href="#"><figure><img src="@Model.ImageUrl1" alt="@Model.ProductName"></figure></a>
                                    </li>
                                    <li>
                                        <a class="active" data-slide-index="2" href="#"><figure><img src="@Model.ImageUrl2" alt="@Model.ProductName"></figure></a>
                                    </li>
                                </ul>
                            </div>
                            <div class="product-image">
                                <figure class="image"><a href="@Model.ImageUrl2" class="lightbox-image" data-fancybox="gallery"><img src="@Model.ImageUrl2" alt="@Model.ProductName"></a></figure>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 col-md-12 col-sm-12 content-column">
                    <div class="product-details">
                        <h2>@Model.ProductName</h2>
                        @* DEĞİŞTİ: seçenek seçilince JS fiyatı güncelleyebilsin diye span'e aldık *@
                        <h3><span id="productPrice" data-base-price="@Model.Price.ToString(CultureInfo.InvariantCulture)">@Model.Price.ToString("N2")</span> ₺</h3>
                        <div class="customer-rating clearfix">
                            <ul class="rating pull-left">
                                <li><i class="icon-Star"></i></li>
                                <li><i class="icon-Star"></i></li>
                                <li><i class="icon-Star"></i></li>
                                <li><i class="icon-Star"></i></li>
                                <li><i class="icon-Star"></i></li>
                            </ul>
                            <div class="review pull-left">
                                <a href="#">( 1 Değerlendirme )</a>
                            </div>
                        </div>
                        <div class="text">
                            <p>@Model.Description</p>
                        </div>
                        @* YENİ: seçenekler butondan ÖNCE seçilsin diye buraya aldık (eskiden aşağıda "Seçenekler:" listesindeydi) *@
                        @{
                            var availableVariants = Model.ProductVariants.Where(x => x.IsAvailable).ToList(); // satışta olmayan seçenek gösterilmesin
                        }
                        @if (availableVariants.Any())
                        {
                            <div class="variant-box">
                                <p>Seçenekler:</p>
                                <div class="variant-options">
                                    @* value boş = seçenek yok; cart.js boş değeri göndermez, handler null alır *@
                                    <label><input type="radio" name="variantId" value="" data-extra-price="0" checked> Standart</label>
                                    @foreach (var variant in availableVariants)
                                    {
                                        <label>
                                            <input type="radio" name="variantId" value="@variant.Id"
                                                   data-extra-price="@variant.AdditionalPrice.ToString(CultureInfo.InvariantCulture)">
                                            @variant.OptionName
                                            @if (variant.AdditionalPrice > 0)
                                            {
                                                <text>(+@variant.AdditionalPrice.ToString("N2") ₺)</text>
                                            }
                                        </label>
                                    }
                                </div>
                            </div>
                        }

                        <div class="addto-cart-box">
                            <ul class="clearfix">
                                @* DEĞİŞTİ: id verdik, placeholder yerine gerçek değer 1 *@
                                <li><input type="text" id="quantity" value="1" inputmode="numeric" maxlength="2" aria-label="Adet"></li>
                                <li><a href="#"><i class="far fa-heart"></i></a></li>
                                @* DEĞİŞTİ: sadece id eklendi *@
                                <li><button type="button" id="addToCartBtn" class="theme-btn-one" data-product-id="@Model.Id">Sepete Ekle</button></li>
                            </ul>
                        </div>



                        <div class="other-links">
                            <ul class="category list">
                                <li>Kategori:</li>
                                <li><a href="#">@Model.Category.CategoryName</a></li>
                            </ul>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>





    <div class="product-discription">
        <div class="auto-container">
            <div class="tabs-box">
               
                <div class="tabs-content">
                    <div class="tab active-tab" id="tab-1">
                        <div class="content-box">
                            <h3>Ürünümüz</h3>
                             <p>@Model.PreperationDescription</p> 
                        </div>
                    </div>
                 
                </div>
            </div>
        </div>
    </div>


    @* YENİ: en çok sipariş edilen 4 ürün (bu ürün hariç) *@
    @await Component.InvokeAsync("_GetTopSellingProductsForDetailViewComponents", new { productId = Model.Id })
</section>
</section>








@section Scripts {
    <script>
        (function () {
            const btn = document.getElementById('addToCartBtn');
            const qtyInput = document.getElementById('quantity');
            const priceEl = document.getElementById('productPrice');
            const basePrice = parseFloat(priceEl.dataset.basePrice);

            // Seçenek değişince ekrandaki fiyatı güncelle.
            // Bu SADECE görüntü; sepete eklenen fiyatı sunucu kendi hesaplıyor (Adım 4).
            document.querySelectorAll('input[name="variantId"]').forEach(radio => {
                radio.addEventListener('change', function () {
                    const total = basePrice + parseFloat(this.dataset.extraPrice || '0');
                    priceEl.textContent = total.toLocaleString('tr-TR', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
                });
            });

            btn.addEventListener('click', async function () {
                const quantity = parseInt(qtyInput.value, 10);
                if (!Number.isInteger(quantity) || quantity < 1) {
                    BageryCart.toast('Geçerli bir adet girin.', false);
                    return;
                }

                const selected = document.querySelector('input[name="variantId"]:checked');
                const variantId = selected ? selected.value : null; // seçenek yoksa null

                btn.disabled = true; // çift tıklamada ürün iki kez eklenmesin
                const result = await BageryCart.add(btn.dataset.productId, quantity, variantId);
                btn.disabled = false;

                // Başarılıysa mesajın yanında "Sepete git" linki çıkar
                BageryCart.toast(result.message, result.success,
                    result.success ? { href: '/Cart/Index', text: 'Sepete git' } : null);

                if (result.success) qtyInput.value = 1;
            });
        })();
    </script>
}